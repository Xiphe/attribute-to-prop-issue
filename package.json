{
  "name": "attribute-to-prop-issue",
  "version": "0.1.0",
  "description": "illustration of an issue with attributes and props",
  "scripts": {
    "build": "stencil build",
    "dev": "sd concurrent \"stencil build --dev --watch\" \"stencil-dev-server\" ",
    "serve": "stencil-dev-server",
    "start": "npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hannes Diercks",
  "license": "UNLICENSED",
  "devDependencies": {
    "@stencil/dev-server": "0.0.18",
    "@stencil/utils": "0.0.4",
  },
  "dependencies": {
    "@stencil/core": "0.2.2"
  }
}
